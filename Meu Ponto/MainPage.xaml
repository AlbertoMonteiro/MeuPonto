<phone:PhoneApplicationPage 
    x:Class="Meu_Ponto.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:Command="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP71"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" DataContext="{Binding MainViewModel, Source={StaticResource Locator}}">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="ApplicationTitle" Text="MEU PONTO" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock x:Name="PageTitle" Text="batida -&gt; hoje" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="98*"/>
                <RowDefinition Height="509*"/>
            </Grid.RowDefinitions>
            <TextBlock HorizontalAlignment="Left" Text="Horário:" VerticalAlignment="Top" Height="27" Width="71" Margin="10,0,0,0"/>
            <TextBox HorizontalAlignment="Left" Height="72" Text="{Binding Horario, Mode=TwoWay}" VerticalAlignment="Top" Width="120" Margin="0,22,0,0"/>
            <Button Content="Registrar" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="120,22,0,0" Height="72" Width="153">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <Command:EventToCommand Command="{Binding AdicionarBatida}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
			<StackPanel Grid.Row="1">
				<Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="200"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Horário" VerticalAlignment="Center"/>
                    <TextBlock Text="Natureza" Grid.Column="1" VerticalAlignment="Center"/>
                    <TextBlock Text="Açoes" Grid.Column="2" HorizontalAlignment="Center"/>
                </Grid>
				<ListBox x:Name="lbBatidas" ItemsSource="{Binding Batidas}">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<Grid>
                        		<Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="200"/>
                        		</Grid.ColumnDefinitions>
								<TextBlock Text="{Binding Horario, StringFormat=\{0:dd/MM/yyyy HH:mm:ss\}}" VerticalAlignment="Center"></TextBlock>
								<TextBlock TextWrapping="Wrap" Text="{Binding NaturezaEntrada}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
								<Button Content="Excluir" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
									<i:Interaction.Triggers>
										<i:EventTrigger EventName="Click">
											<Command:EventToCommand Command="{Binding ElementName=lbBatidas, Path=DataContext.RemoverBatida}" CommandParameter="{Binding}" PassEventArgsToCommand="False" />
										</i:EventTrigger>
									</i:Interaction.Triggers>
								</Button>
							</Grid>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</StackPanel>
			<TextBlock HorizontalAlignment="Left" Margin="278,10,0,0" TextWrapping="Wrap" Text="{Binding HorarioTrabalhado, StringFormat=Horário trabalhado: \{0\}}" VerticalAlignment="Top" Height="78" Width="168" FontSize="18.667" TextAlignment="Center" FontWeight="Bold"/>
        </Grid>
    </Grid>

</phone:PhoneApplicationPage>